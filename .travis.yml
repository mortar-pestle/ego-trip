dist: xenial
sudo: required
language: csharp
solution: "ego-trip.sln"
before_install:
  - chmod a+x ./scripts/install.sh
install:
  - pwd
  - ls
  - cd scripts
  - ls
  - sh ./install.sh
  - cd ..
  - cd ego-trip
  - ls
